# Synthetic PCF Resolution

Seeded downstream compatibility pack for `synthetic_pcf.resolution.v1`.

This pack represents an accepted synthetic PCF resolution replay case. Its
prepared canonical bundle exercises receipt-gated replay and public projection
through `comp.scenario_contracts`.

The pack is review evidence only. It does not authorize projection and does not
replace `comp` receipt replay.
