# Synthetic PCF Anomaly

Seeded downstream compatibility pack for `synthetic_pcf.anomaly.v1`.

This pack represents a blocked synthetic PCF replay case. Its prepared canonical
bundle exercises the no-projection path through `comp.scenario_contracts`.

The pack is review evidence only. It does not authorize projection and does not
replace `comp` receipt replay.
