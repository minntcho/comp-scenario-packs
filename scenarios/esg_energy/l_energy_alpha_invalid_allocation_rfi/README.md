# L-Energy Alpha Invalid Allocation RFI

Seeded downstream compatibility pack for `l_energy.alpha_invalid_allocation_rfi.v1`.

This pack represents a blocked allocation-method path. Its canonical bundle has
no receipts and no public projections because `comp` should not authorize public
output when the source path remains blocked.

The pack is review evidence only. It does not authorize projection and does not
replace `comp` receipt replay.
