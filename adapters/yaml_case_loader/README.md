# YAML Case Loader

Status: planned

This adapter area is reserved for loading external YAML scenario cases into
scenario-pack fixtures.

The loader should translate product/platform data into artifacts that `comp`
can validate through public APIs. It must not import private `comp`
implementation modules.
